[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dix"
version = "0.1.0"
description = "Declarative Interface eXecutor"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "dix contributors" }]
dependencies = [
  "fastapi>=0.115",
  "jinja2>=3.1",
  "pydantic>=2.8",
  "typer>=0.12",
  "uvicorn>=0.30",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "httpx>=0.27",
]

[project.scripts]
dix = "dix.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
