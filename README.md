# dix

Declarative Interface eXecutor.

`dix` provides spec-driven interfaces for controlled actions. This foundation cut focuses on a
small end-to-end path: configuration, interface specs, core components, a reference HTML renderer,
and runtime state updates.

## Development

```bash
uv run --extra dev pytest
uv run dix --help
uv run dix config effective
```
