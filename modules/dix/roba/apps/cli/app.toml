[app]
id = "cli"

[compositions.typer]
use = "dix/cli/typer"

[apps.daemon]
use = "dix/roba/daemon"

[apps.control]
use = "dix/roba/control"

[apps.managed]
use = "dix/roba/managed"

[functions.main]
description = "Run the explicit DIX ROBA daemon CLI."
