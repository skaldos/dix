[composition]
id = "ipc"

[functions.focused_con_id]
description = "Return the integer ID of the currently focused Sway container."

[functions.focus_direction]
description = "Move focus once through one native Sway direction."

[functions.focus_con_id]
description = "Focus one Sway container by its integer ID."

[functions.live_con_ids]
description = "Return detached integer IDs of the current Sway tree leaves."
