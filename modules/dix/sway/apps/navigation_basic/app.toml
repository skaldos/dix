[app]
id = "navigation_basic"

[compositions.ipc]
use = "dix/sway/ipc"

[functions.left]
description = "Move focus once through Sway's native left direction."

[functions.right]
description = "Move focus once through Sway's native right direction."

[functions.up]
description = "Move focus once through Sway's native up direction."

[functions.down]
description = "Move focus once through Sway's native down direction."
