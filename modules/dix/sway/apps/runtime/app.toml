[app]
id = "runtime"

[apps.managed]
use = "dix/roba/managed"

[apps.control]
use = "dix/roba/control"

[apps.daemon]
use = "dix/roba/daemon"

[functions.start]
description = "Start the owner-specific Sway runtime."

[functions.status]
description = "Read the owner-specific Sway runtime status without credentials."

[functions.stop]
description = "Stop the owner-specific Sway runtime."
