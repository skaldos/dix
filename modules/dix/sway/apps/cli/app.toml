[app]
id = "cli"

[compositions.typer]
use = "dix/cli/typer"

[apps.groups]
use = "dix/sway/groups"

[apps.runtime]
use = "dix/sway/runtime"

[functions.main]
description = "Run the explicit DIX Sway runtime and groups CLI."
