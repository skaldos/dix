[app]
id = "state"

[compositions.states]
use = "acme/state_demo/combined"

[functions.snapshot]
description = "Return the two independent owner states and local reaction count."

[functions.pressure]
description = "Exercise explicit local state composition boundaries."
