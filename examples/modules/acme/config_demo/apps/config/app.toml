[app]
id = "config"

[compositions.config_model]
use = "dix/config/pydantic"

[functions.validate]
description = "Validate owner-provided values with the application-owned config model."

[functions.pressure]
description = "Exercise valid and invalid owner-controlled config values."
